<% if @wiki.private?  %>
    <%= form_for [wiki, Collaborator.new] do |f| %>
       <br> 
        <p>Add Collaborator</p>
        <%= f.label :user %>
        <%= f.text_field :user, placeholder: "User Email" %>
        <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
<% end %>